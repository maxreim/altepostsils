document.addEventListener('DOMContentLoaded',() => {const defaultLang = 'de'; const getLangFromURL = () => {const urlParams = new URLSearchParams(window.location.search); return urlParams.get('lang'); }; const urlLang = getLangFromURL(); const storageLang = localStorage.getItem('lang'); let currentLang = urlLang || storageLang || defaultLang; const skipInitialTranslation = (currentLang === 'de' && !urlLang && !storageLang); if (urlLang && translations[urlLang]) {localStorage.setItem('lang',urlLang); }const applyTranslations = (lang,root = document) => {if (!translations[lang]) return; root.querySelectorAll('[data-i18n]').forEach((el) => {const key = el.getAttribute('data-i18n'); const translation = translations[lang][key]; if (translation) {if (el.innerHTML === translation || el.textContent === translation) return; if (el.tagName === 'META') {el.setAttribute('content',translation); }else if (el.tagName === 'TITLE') {if (document.title !== translation) document.title = translation; }else if (el.tagName === 'INPUT' || el.tagName === 'TEXTAREA') {if (el.hasAttribute('placeholder')) {if (el.placeholder !== translation) el.placeholder = translation; }else if (el.type === 'submit' || el.type === 'button') {if (el.value !== translation) el.value = translation; }}else {if (translation.indexOf('<') === -1) {el.textContent = translation; }else {el.innerHTML = translation; }}}}); if (root === document) {document.documentElement.lang = lang; document.querySelectorAll('.lang-switcher button').forEach(btn => {if (btn.getAttribute('data-lang') === lang) {btn.classList.add('active'); btn.setAttribute('aria-current','true'); }else {btn.classList.remove('active'); btn.removeAttribute('aria-current'); }}); }}; window.i18n = {apply: applyTranslations,getLang: () => currentLang }; const setupLangSwitcher = () => {const switchers = document.querySelectorAll('.lang-switcher button'); switchers.forEach(btn => {btn.addEventListener('click',(e) => {const lang = e.target.getAttribute('data-lang'); if (lang && translations[lang]) {currentLang = lang; localStorage.setItem('lang',lang); const newUrl = new URL(window.location.href); if (lang === defaultLang) {newUrl.searchParams.delete('lang'); }else {newUrl.searchParams.set('lang',lang); }window.history.pushState({lang },"",newUrl); applyTranslations(lang); }}); }); }; if (typeof translations !== 'undefined') {setupLangSwitcher(); if (!skipInitialTranslation) {applyTranslations(currentLang); }else {document.documentElement.lang = 'de'; document.querySelectorAll('.lang-switcher button[data-lang="de"]').forEach(btn => {btn.classList.add('active'); btn.setAttribute('aria-current','true'); }); }}});