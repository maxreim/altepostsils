document.addEventListener('DOMContentLoaded',() => {const progressBar = document.createElement('div'); progressBar.className = 'scroll-progress'; document.body.appendChild(progressBar); const updateScrollProgress = () => {const winScroll = document.body.scrollTop || document.documentElement.scrollTop; const height = document.documentElement.scrollHeight - document.documentElement.clientHeight; const scrolled = (winScroll / height) * 100; progressBar.style.width = scrolled + "%"; }; const navbar = document.querySelector('.navbar'); const backToTop = document.querySelector('.back-to-top'); const sentinel = document.createElement('div'); sentinel.id = 'scroll-sentinel'; sentinel.style.cssText = 'position:absolute; top:0; height:50px; width:1px; pointer-events:none; visibility:hidden;'; document.body.prepend(sentinel); const navObserver = new IntersectionObserver((entries) => {const isScrolling = !entries[0].isIntersecting; navbar.classList.toggle('scrolled',isScrolling); },{threshold: 0 }); navObserver.observe(sentinel); let scrollTicking = false; window.addEventListener('scroll',() => {if (!scrollTicking) {window.requestAnimationFrame(() => {updateScrollProgress(); if (backToTop) {if (window.scrollY > 500) {backToTop.classList.add('visible'); }else {backToTop.classList.remove('visible'); }}scrollTicking = false; }); scrollTicking = true; }},{passive: true }); const mobileToggle = document.querySelector('.mobile-toggle'); const navLinks = document.querySelector('.nav-links'); if (mobileToggle && navLinks) {mobileToggle.addEventListener('click',() => {navLinks.classList.toggle('active'); mobileToggle.innerHTML = navLinks.classList.contains('active') ? '✕' : '☰'; }); document.querySelectorAll('.nav-links a').forEach(link => {link.addEventListener('click',() => {navLinks.classList.remove('active'); mobileToggle.innerHTML = '☰'; }); }); }const sections = document.querySelectorAll('section,header'); const navLinksItems = document.querySelectorAll('.nav-links a'); const reveals = document.querySelectorAll('.reveal'); const revealObserver = new IntersectionObserver((entries) => {entries.forEach(entry => {if (entry.isIntersecting) {entry.target.classList.add('active'); revealObserver.unobserve(entry.target); }}); },{threshold: 0.1,rootMargin: '0px 0px -50px 0px' }); reveals.forEach(r => revealObserver.observe(r)); const sectionObserver = new IntersectionObserver((entries) => {entries.forEach(entry => {if (entry.isIntersecting) {const id = entry.target.getAttribute('id'); if (id) {navLinksItems.forEach(link => {link.classList.toggle('active',link.getAttribute('href') === `#${id}`); }); }}}); },{threshold: 0.5 }); sections.forEach(s => sectionObserver.observe(s)); const roomCards = document.querySelectorAll('.room-card'); const modalOverlay = document.querySelector('.modal-overlay'); const modalClose = document.querySelector('.modal-close'); const modalBody = document.querySelector('.modal-body'); if (modalOverlay && modalClose && modalBody) {const roomKeys = ['wohnzimmer','kueche','schlafzimmer','bad','gang','allgemein']; const roomContents = {}; roomKeys.forEach(key => {const tmpl = document.getElementById(`content-${key}`); roomContents[key] = tmpl ? tmpl.innerHTML : ''; }); const openRoomModal = (roomKey) => {if (!roomContents[roomKey]) return; modalBody.innerHTML = roomContents[roomKey]; const currentIndex = roomKeys.indexOf(roomKey); const prevIndex = (currentIndex - 1 + roomKeys.length) % roomKeys.length; const nextIndex = (currentIndex + 1) % roomKeys.length; const navDiv = document.createElement('div'); navDiv.className = 'modal-nav'; const prevBtn = document.createElement('button'); prevBtn.className = 'modal-nav-btn'; prevBtn.innerHTML = `← ${window.translations?.[window.i18n?.getLang()]?.['room_' + roomKeys[prevIndex]] || 'Back'}`; prevBtn.onclick = () => openRoomModal(roomKeys[prevIndex]); const nextBtn = document.createElement('button'); nextBtn.className = 'modal-nav-btn'; nextBtn.innerHTML = `${window.translations?.[window.i18n?.getLang()]?.['room_' + roomKeys[nextIndex]] || 'Next'}→`; nextBtn.onclick = () => openRoomModal(roomKeys[nextIndex]); navDiv.appendChild(prevBtn); navDiv.appendChild(nextBtn); modalBody.appendChild(navDiv); if (window.i18n) window.i18n.apply(window.i18n.getLang(),modalBody); modalOverlay.classList.add('active'); document.body.style.overflow = 'hidden'; attachLightboxEvents(); modalClose.focus(); }; roomCards.forEach(card => {card.addEventListener('click',() => openRoomModal(card.getAttribute('data-room'))); }); const closeModal = () => {modalOverlay.classList.remove('active'); document.body.style.overflow = ''; }; modalClose.addEventListener('click',closeModal); modalOverlay.addEventListener('click',(e) => {if (e.target === modalOverlay) closeModal(); }); const lightbox = document.createElement('div'); lightbox.className = 'lightbox'; lightbox.innerHTML = ` <div class="lightbox-close">✕</div> <div class="lightbox-arrow lb-prev" style="position:absolute; left:20px; color:#fff; font-size:3rem; cursor:pointer;">←</div> <img src="" style="max-width:90%; max-height:90vh;"> <div class="lightbox-arrow lb-next" style="position:absolute; right:20px; color:#fff; font-size:3rem; cursor:pointer;">→</div> `; document.body.appendChild(lightbox); const lImg = lightbox.querySelector('img'); const attachLightboxEvents = () => {const imgs = Array.from(modalBody.querySelectorAll('.modal-gallery img')); imgs.forEach((img,i) => {img.onclick = () => {lImg.src = img.src; lImg.dataset.index = i; lightbox.classList.add('active'); }; }); lightbox.querySelector('.lb-prev').onclick = (e) => {e.stopPropagation(); let i = (parseInt(lImg.dataset.index) - 1 + imgs.length) % imgs.length; lImg.src = imgs[i].src; lImg.dataset.index = i; }; lightbox.querySelector('.lb-next').onclick = (e) => {e.stopPropagation(); let i = (parseInt(lImg.dataset.index) + 1) % imgs.length; lImg.src = imgs[i].src; lImg.dataset.index = i; }; }; lightbox.onclick = () => lightbox.classList.remove('active'); document.addEventListener('keydown',(e) => {if (e.key === 'Escape') {lightbox.classList.remove('active'); closeModal(); }if (lightbox.classList.contains('active')) {const imgs = Array.from(modalBody.querySelectorAll('.modal-gallery img')); let i = parseInt(lImg.dataset.index); if (e.key === 'ArrowRight') {i = (i + 1) % imgs.length; lImg.src = imgs[i].src; lImg.dataset.index = i; }if (e.key === 'ArrowLeft') {i = (i - 1 + imgs.length) % imgs.length; lImg.src = imgs[i].src; lImg.dataset.index = i; }}}); }const lazyIframes = document.querySelectorAll('.iframe-container[data-src]'); const loadIframe = (container) => {const src = container.getAttribute('data-src'); const title = container.getAttribute('data-title'); const height = container.getAttribute('data-height') || '500'; const allow = container.getAttribute('data-allow') || ''; container.innerHTML = ` <iframe title="${title}" src="${src}" width="100%" height="${height}" style="border:0;" allowfullscreen="" loading="lazy" allow="${allow}"></iframe> `; container.classList.add('loaded'); }; const iframeObserver = new IntersectionObserver((entries,observer) => {entries.forEach(entry => {if (entry.isIntersecting) {loadIframe(entry.target); observer.unobserve(entry.target); }}); },{rootMargin: '200px' }); lazyIframes.forEach(c => iframeObserver.observe(c)); const getEaster = (year) => {const f = Math.floor,G = year % 19,C = f(year / 100),H = (C - f(C / 4) - f((8 * C + 13) / 25) + 19 * G + 15) % 30,I = H - f(H / 28) * (1 - f(H / 28) * f(29 / (H + 1)) * f((21 - G) / 11)),J = (year + f(year / 4) + I + 2 - C + f(C / 4)) % 7,L = I - J,month = 3 + f((L + 40) / 44),day = L + 28 - 31 * f(month / 4); return new Date(year,month - 1,day); }; const getSeason = (date) => {const year = date.getFullYear(); const month = date.getMonth(); const day = date.getDate(); const easter = getEaster(year); const dayOfYear = (Date.UTC(year,month,day) - Date.UTC(year,0,0)) / 86400000; const easterDayOfYear = (Date.UTC(year,easter.getMonth(),easter.getDate()) - Date.UTC(year,0,0)) / 86400000; if (dayOfYear <= 7 || dayOfYear >= 355 || (dayOfYear >= 32 && month < 3 && date.getTime() < easter.getTime())) return 'S1W'; if (dayOfYear > 7 && dayOfYear < 32) return 'S2W'; if (date >= easter && month <= 4) return 'S4'; if (month === 5) return 'S3'; if (month >= 6 && month <= 7) return 'S1S'; if (month === 8 && day <= 15) return 'S1S'; if ((month === 8 && day > 15) || (month === 9 && day <= 20)) return 'S2S'; if ((month === 9 && day > 20) || (month === 10) || (month === 11 && day < 21)) return 'S4'; return 'S4'; }; const seasonRates = {S1W: 2030,S1S: 1950,S2W: 1830,S2S: 1790,S3: 1640,S4: 1440 }; const currentSeason = getSeason(new Date()); document.querySelectorAll(`[data-season="${currentSeason}"]`).forEach(el => el.classList.add('current-season')); const form = document.querySelector('#kontakt form'); if (form) {const dateIn = form.querySelector('input[name="Anreise"]'); const dateOut = form.querySelector('input[name="Abreise"]'); form.onsubmit = (e) => {const lang = window.i18n ? window.i18n.getLang() : 'de'; const t = window.translations?.[lang] || window.translations['de']; if (dateIn && dateOut && dateIn.value && dateOut.value) {if (new Date(dateOut.value) <= new Date(dateIn.value)) {e.preventDefault(); alert(t.date_error); return; }}e.preventDefault(); const btn = form.querySelector('button'); const originalBtnText = btn.innerHTML; btn.disabled = true; btn.innerHTML = '<span class="loader"></span>'; const formData = new FormData(form); fetch(form.action,{method: "POST",headers: {'Accept': 'application/json' },body: formData }) .then(r => {if (r.ok) {const successMsg = window.translations?.[window.i18n?.getLang()]?.form_success || "Danke!"; form.innerHTML = `<div class="form-feedback success">${successMsg}</div>`; }else {return r.json().then(errData => {console.error('FormSubmit Error Response:',errData); throw new Error(errData.message || 'Submission failed'); }).catch(() => {throw new Error('Submission failed'); }); }}) .catch((err) => {console.error('Form Submission Error:',err); const errorMsg = window.translations?.[window.i18n?.getLang()]?.form_error || "Error"; const existingError = form.querySelector('.form-feedback.error'); if (existingError) existingError.remove(); const errorDiv = document.createElement('div'); errorDiv.className = 'form-feedback error'; errorDiv.innerHTML = errorMsg; form.prepend(errorDiv); btn.disabled = false; btn.innerHTML = originalBtnText; }); }; }});